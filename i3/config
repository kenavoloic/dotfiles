# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# 9/08/2018
#Nécessite l'installation de feh, nitrogen, font Inconsolata, rxvt-unicode
#Utilise également le fichier i3blocks
#Cf .Xdefaults pour les réglages de rxvt

set $nsi --no-startup-id 

#Désactive les bords de fenêtre
#for_window [class="^.*"] border pixel2
#hide_edge_borders smart
hide_edge_borders smart #none #both # none

#active numlock
exec $nsi numlockx on

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:monospace 10
#font pango:Inconsolata 14
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
# font pango:DejaVu Sans Mono 14

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
#bindsym $mod+Return exec xterm
#bindsym $mod+Return exec rxvt
bindsym $mod+Return exec $nsi rxvt-unicode -name 'rxvt-unicode'
#bindsym $mod+Return exec $nsi terminator
#bindsym $mod+Return exec $nsi termit
bindsym $mod+Control+e exec $nsi emacs
bindsym $mod+Control+t exec $nsi thunar
bindsym $mod+Control+c exec $nsi chromium

# kill focused window
bindsym $mod+Shift+a kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
# bindsym $mod+j focus left
# bindsym $mod+k focus down
# bindsym $mod+l focus up
# bindsym $mod+m focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
# bindsym $mod+Shift+j move left
# bindsym $mod+Shift+k move down
# bindsym $mod+Shift+l move up
# bindsym $mod+Shift+m move right

# alternatively, you can use the cursor keys:
# bindsym $mod+Shift+Left move left
# bindsym $mod+Shift+Down move down
# bindsym $mod+Shift+Up move up
# bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+z layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+q focus parent

# focus the child container
#bindsym $mod+d focus child

set $bureau1 1
set $bureau2 2
set $bureau3 3
set $bureau4 4
set $bureau5 5
set $bureau6 6
set $bureau7 7
set $bureau8 8
set $bureau9 9
set $bureau10 10


# switch to workspace
bindsym $mod+ampersand workspace $bureau1
bindsym $mod+eacute workspace $bureau2
bindsym $mod+quotedbl workspace $bureau3
bindsym $mod+apostrophe workspace $bureau4
bindsym $mod+parenleft workspace $bureau5
bindsym $mod+minus workspace $bureau6
bindsym $mod+egrave workspace $bureau7
bindsym $mod+underscore workspace $bureau8
bindsym $mod+ccedilla workspace $bureau9
bindsym $mod+agrave workspace $bureau10

# move focused container to workspace
bindsym $mod+Shift+ampersand move container to workspace $bureau1
bindsym $mod+Shift+eacute move container to workspace $bureau2
bindsym $mod+Shift+quotedbl move container to workspace $bureau3
bindsym $mod+Shift+apostrophe move container to workspace $bureau4
bindsym $mod+Shift+5 move container to workspace $bureau5
bindsym $mod+Shift+minus move container to workspace $bureau6
bindsym $mod+Shift+egrave move container to workspace $bureau7
bindsym $mod+Shift+underscore move container to workspace $bureau8
bindsym $mod+Shift+ccedilla move container to workspace $bureau9
bindsym $mod+Shift+agrave move container to workspace $bureau10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Quitter i3 ? Cela clôturera votre session X.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	position bottom
        status_command i3status
	font pango:Inconsolata 12
	separator_symbol "|"
	tray_padding 0
	workspace_buttons yes
	status_command i3status -c ~/.config/i3/i3status.conf

	#N'affiche la barre que si $mod est appuyée.
	#mode hide
	#hidden_state hide
	#modifier $mod
#i3bar_command i3bar
#status_command /usr/bin/i3blocks -c ~/.config/i3/i3blocks.conf
colors {
statusline #FFFFFF
background #000008
separator #F3370F
#                  border   background text
focused_workspace #00000000 #00000000 #C6FF00
inactive_workspace #00000000 #00000000 #DDDDDD
active_workspace #00000000 #00000000 #FFFFFF
urgent_workspace #B71C1C #B71C1C #000000
binding_mode #B71C1C #B71C1C #000000

}
}

# amixer -q -D "default" sset 'Master' 5%+
# amixer -q -D "default" sset 'Master' 5%-
# amixer -q -D "default" sset 'Master' toggle
# exec $nsi pactl set-sink-volume 0 +5%
# exec $nsi pactl set-sink-volume 0 -5%
# exec $nsi pactl set-sink-mute 0 toggle

# bindsym $mod+Shift+Up exec $nsi pactl set-sink-volume 0 +5%
# bindsym $mod+Shift+Down exec $nsi pactl set-sink-volume 0 -5%
# bindsym $mod+m exec $nsi pactl set-sink-mute 0 toggle

#mixer
bindsym $mod+Shift+Up exec $nsi pactl set-sink-volume 0 +2%
bindsym $mod+Shift+Down exec $nsi pactl set-sink-volume 0 -2%
bindsym $mod+m exec $nsi pactl set-sink-mute 0 toggle
#mixer clavier portable
bindsym XF86AudioRaiseVolume exec $nsi pactl set-sink-volume 0 +2%
bindsym XF86AudioLowerVolume exec $nsi pactl set-sink-volume 0 -2%
bindsym XF86AudioMute exec $nsi pactl set-sink-mute 0 toggle
# Mocp
bindsym $mod+Control+Up exec $nsi mocp -p
bindsym $mod+Control+Down exec $nsi mocp -P

#wallpaper
exec_always $nsi nitrogen --restore

####################################

#bindsym $mod+l exec "slock"


#bindsym $mod+Control+e exec $nsi emacs
#bindsym $mod+Control+c exec $nsi chromium
#bindsym $mod+g  exec $nsi conkeror
#bindsym $mod+Control+t exec $nsi thunar


#toujours ouvrir sur

assign[class="emacs"] $bureau1
assign[class="Chromium"] $bureau2
assign[class="Conkeror"] $bureau2

assign [class="qGo"] $bureau5
for_window [class="qGo"] floating enable border normal
#for_window [class="qGo"] focus

#assign[class=""] $bureau1
#assign[class=""] $bureau2
#assign[class=""] $bureau3
#assign[class=""] $bureau4

